// swift-tools-version:5.3
import PackageDescription

let package = Package(
   name: "<lib/>",
   platforms: [
     .iOS(.v14),
   ],
   products: [
      .library(name: "<lib/>", targets: ["<lib/>"])
   ],
   targets: [
      .binaryTarget(
         name: "<lib/>",
         path: "./<lib/>.xcframework")
   ]
)